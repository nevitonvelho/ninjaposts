# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Build das Cloud Functions
functions/lib

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Exceção: parâmetros das Functions (nomes de modelo, não segredo). Precisam
# estar versionados, senão todo deploy em máquina limpa falha na resolução dos
# `defineString`. A chave da OpenAI não está aqui — é `defineSecret`.
!functions/.env

# Capturas de tela e anexos locais
Captura de tela*.png
*.local.png

# Credenciais de service account — nunca commitar
serviceAccount*.json
*-firebase-adminsdk-*.json
